import React{{#if wantPropTypes}}, {PropTypes}{{/if}} from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const {{name}} = (props) => {
  return (
    <View>
      <Text>{{name}}</Text>
    </View>
  );
};

{{#if wantPropTypes}}
{{name}}.propTypes = {

};
{{/if}}

export default {{name}};
